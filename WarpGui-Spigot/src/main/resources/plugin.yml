name: ${project.name}
main: ${project.groupId}.WarpGuiSpigot
version: ${project.version}
author: Topeka_
api-version: 1.16
commands:
  warp:
    description: warp command
    usage: /warp
    permission: warpgui.warp.use
    permission-message: You haven't access to this command
  lobby:
    description: lobby command
    usage: /lobby
    permission: warpgui.lobby.use
    permission-message: You haven't access to this command